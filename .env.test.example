# Test Environment Configuration (Example)
# Copy this file to .env.test and fill in your test database credentials

# Test Database (MUST be separate from development database)
# This prevents tests from deleting your development data
DATABASE_URL="postgresql://username:password@localhost:5432/stockmate_test"

# NextAuth (can use same values as .env)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="" # Generate with: openssl rand -base64 32

# KIS API (can use same values as .env)
KIS_APP_KEY=""
KIS_APP_SECRET=""
KIS_API_URL="https://openapivts.koreainvestment.com:29443"

# Setup Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Update DATABASE_URL with your test database credentials
# 3. Create test database: CREATE DATABASE stockmate_test;
# 4. Run migrations: npx cross-env DATABASE_URL="your_test_db_url" npx prisma migrate deploy
# 5. Run tests: npm test
